<h1 class="container">List</h1>

<% @lists.each do |list| %>
<div class="card-category container my-4" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3))">
  <%= link_to list.name, list_path(list.id) %>
</div>
<% end %>
